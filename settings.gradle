/*
 * This settings file was auto generated by the Gradle buildInit task
 * by 'j3r0lin' at '10/15/15 2:41 AM' with Gradle 2.7
 *
 * The settings file is used to specify which projects to include in your build.
 * In a single project build this file can be empty or even removed.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user guide at https://docs.gradle.org/2.7/userguide/multi_project_builds.html
*/

import static groovy.io.FileType.DIRECTORIES
import static groovy.io.FileVisitResult.SKIP_SUBTREE

rootProject.name = 'LC_AP_AuthAPI'

def skipDirs = ~/^(\.|gitconfig|config|build|src|out|target|gradle|template|libs|project-setting)/

def preDir = {
    if (skipDirs.matcher(it.name).matches())
        return SKIP_SUBTREE
}

rootDir.traverse(type: DIRECTORIES, preDir: preDir, maxDepth: 0) { dir ->
    if ((!skipDirs.matcher(dir.name))) {
        include dir.name
    }
}
